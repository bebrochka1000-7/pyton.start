x=0
y=300
def setup():
    global x,y
    fill(155,33,33)
    background(255)
    textSize(30)
    size(600,600)
def draw():
    global x,y

def keyPressed():
    global x,y
    text(key,x,y)
    x=x+15
    if(key==ENTER):
        y=y+30
        x=0
    if(key==TAB):
        x=x+40
    
    



